---
to_version: 0.7.0
from_version: 0.6.7
title: "Marker migration for the token-discipline + updater release"
breaking: false
revertible: true
estimated_duration: "<1 second"
touches:
  - workspace/.version
helper_scripts: {}
---

## Summary

Marker migration: 0.7.0 ships framework-side changes only — four new rules
(subagent-bulk-reads, large-file-reads, tool-usage-corrections,
machine-local-home), the consumer-side `refresh` skill, the
`icloud_dup_check` prelude probe, the `skill_loader` prompt hook, and the
machine-local root `~/.superagent/` (tmp + tools). No workspace data
changes shape; this migration only records the version step. The
machine-local root is created lazily by the tools that use it (or by
`uv run python -m superagent.tools.home`).

## Pre-flight checks

- [ ] Workspace exists at the resolved path.
- [ ] `workspace/.version` reads any `0.6.x` with `x >= 1` (the 0.6.2–0.6.7
      releases carried no migrations, so a PATCH gap below 0.7.0 is
      expected and tolerated per `contracts/versioning.md`).

## Migrate

1. Write `0.7.0` to `workspace/.version`.

## Validate

- [ ] `workspace/.version` reads `0.7.0`.
- [ ] `uv run python -m superagent.tools.version check` exits 0.

## Revert

1. Write the pre-migration `0.6.x` value recorded during pre-flight
   (`0.6.7` on an up-to-date checkout) back to `workspace/.version`.
