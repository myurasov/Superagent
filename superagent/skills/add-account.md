---
name: superagent-add-account
description: >-
  Register a financial / utility / subscription account in `accounts-index.yaml`.
  Captures last-4 only; full credentials live in your password manager and
  are referenced by `vault_ref`.
triggers:
  - add an account
  - register a bank / card / brokerage / utility / subscription account
  - I opened a new <account>
mcp_required: []
mcp_optional: []
cli_required: []
cli_optional: []
---





<!-- step-index:start -->

## Step index

_Auto-generated by `tools/add_step_index.py`. Re-run on any edit._

| # | Step | Lines |
|---|------|-------|
| 1 | Collect the account's data | 22-53 |
| 2 | Check for duplicates | 55-57 |
| 3 | Append the account row | 59-87 |
| 4 | Cross-link | 89-93 |
| 5 | Optional — wire to a finance ingestor | 95-103 |
| 6 | Update Domain | 105-114 |
| 7 | Confirm | 116-122 |
| 8 | Logging | 124-133 |

<!-- step-index:end -->

# Superagent add-account skill

## 1. Collect the account's data

Use `AskQuestion` for the kind question; inline for the rest.

- **Name** (e.g. "Chase Total Checking", "Fidelity 401(k)", "PG&E electric").
- **Institution** (bank / brokerage / utility / subscription provider).
- **Kind** (single-select):
  - checking
  - savings
  - credit_card
  - brokerage
  - retirement (401k / 403b / IRA / Roth / pension)
  - loan
  - mortgage
  - utility
  - insurance
  - subscription
  - membership
  - wallet (Apple Pay / Google Pay reference)
  - hsa / fsa / hsa_invest
  - other
- **Number last 4** (for matching transactions). NEVER ask for the full number.
- **Vault ref** — where the full credential lives. Ask the user for the password-manager item identifier (e.g. `1Password://Personal/chase-checking`, `keychain:bofa-savings`). Optional but encouraged.
- **URL** (login URL).
- **Primary holder** — `self` | `joint` | `spouse` | `child:<name>` | other.
- **Currency** (default `config.preferences.money.currency`).
- **Opened date** (ISO 8601 date or null).
- **Beneficiaries** (relevant for retirement / life insurance — list of names).
- **Low-balance threshold** (for checking / savings; defaults to `config.preferences.money.low_balance_default`).
- **Notes** — APR, terms, special instructions.

Auto-derive **id** = `<kind>-<institution-short>-<last4 or other discriminator>` (e.g. `checking-chase-1234`, `401k-fidelity`, `electric-pge`).

## 2. Check for duplicates

Search `_memory/accounts-index.yaml.accounts[]` by `id` and by `(institution, kind, number_last4)`. Confirm if similar.

## 3. Append the account row

```yaml
- id: "<id>"
  name: "<Name>"
  institution: "<Institution>"
  kind: "<kind>"
  number_last4: "<last 4>"
  vault_ref: "<vault_ref>"
  url: "<url>"
  primary_holder: "<holder>"
  currency: "<currency>"
  opened: <date or null>
  closed: null
  status: "active"
  beneficiaries: <list>
  linked_bills: []
  linked_subs: []
  low_balance_threshold: <number or null>
  plaid_item_id: null
  ingest_source: "manual"
  transactions: []           # populated as payments flow per contracts/payment-confirmations.md § 4 step 3a
  notes: "<notes>"
  tags: []
  created: <now>
  last_updated: <now>
```

`transactions[]` stays empty on first add. Every payment skill that touches this account (`bills mark-paid`, `subscriptions log-renewal`, `expenses` ingest, etc.) is responsible for appending a structured row per `contracts/payment-confirmations.md § 4.3`. Schema lives in `superagent/templates/memory/accounts-index.yaml` comments. Ingestor-sourced rows are the ingestor's native shape; user-reported and agent-initiated payments are mirrored from the entity-side capture in the same skill turn.

## 4. Cross-link

Ask:
- **"Are any existing bills paid from this account?"** — for each yes, set `linked_bills` here AND `bills.yaml.<bill>.pay_from_account = <account-id>`.
- **"Are any existing subscriptions charged to this account?"** — same for subs.

## 5. Optional — wire to a finance ingestor

If `_memory/data-sources.yaml` has Plaid or Monarch enabled:

> "Want to link this account to **Plaid / Monarch** so transactions get auto-ingested? (yes / later / never)"

On `yes`:
- For Plaid: invoke `uv run python superagent/tools/ingest/plaid.py --link-account <id>` (which walks the user through the link flow and sets `plaid_item_id`).
- For Monarch: invoke `uv run python superagent/tools/ingest/monarch.py --link-account <id>`.

## 6. Update Domain

If `domain` for this account is `finances` (the typical case), append an entry to `Domains/Finances/history.md`:

```markdown
#### <YYYY-MM-DD> — Added account: <Name>

<Kind> at <Institution>; ending <last4>; <URL>.
Vault ref: <ref>.
```

## 7. Confirm

```
Added account "<Name>" (id: <id>).
Linked bills: <count>; linked subscriptions: <count>.
Ingest: <plaid/monarch/manual>.
```

## 8. Logging

Append to `interaction-log.yaml`:

```yaml
- timestamp: <now>
  type: skill_run
  subject: "add-account"
  summary: "Added <kind> account at <Institution> (id: <id>)."
  related_account: <id>
```
