---
name: superagent-todo
description: >-
  Manage the Superagent task list in `_memory/todo.yaml`: add, list, complete,
  update tasks, and triage overdue tasks (force a decision on every overdue
  row). P0-P3 priority rules with overdue awareness; auto-syncs the
  workspace `todo.md` and per-domain `status.md` on every change.
triggers:
  - User asks to add or list or complete or update tasks
  - Mentions of todo.yaml or task list or P0-P3 priorities
  - what is due
  - show open tasks
  - mark task done
  - todo triage
  - triage overdue
  - clean up overdue tasks
  - deal with overdue tasks
mcp_required: []
mcp_optional: []
cli_required: []
cli_optional: []
---

<!-- step-index:start -->

## Step index

_Auto-generated by `tools/add_step_index.py`. Re-run on any edit._

| # | Step | Lines |
|---|------|-------|
| 1 | Load configuration | 46-49 |
| 2 | Load the task file | 50-53 |
| 3 | Branch on user intent | 54-132 |
| 4 | Persist | 133-136 |
| 5 | Sync `status.md` / `todo.md` files | 137-161 |
| 6 | P0 highlighting | 162-174 |
| 7 | Logging | 175-187 |

<!-- step-index:end -->

# Superagent todo skill

Follow these steps in order; skip steps that don't apply.

## 1. Load configuration

Read `workspace/_memory/config.yaml` to resolve the workspace root.

## 2. Load the task file

Read `workspace/_memory/todo.yaml`. If missing, initialize from `superagent/templates/memory/todo.yaml` (preserve `schema_version`), then continue.

## 3. Branch on user intent

### Add a task

When the user asks to add a task:

1. Collect or infer:
   - **title** (mandatory; ≤ 80 chars).
   - **description** (optional; details, links, acceptance criteria).
   - **priority** (P0 / P1 / P2 / P3). Default P2. Use heuristics:
     - "today" / "urgent" / "now" / "ASAP" → P0.
     - "this week" / "by Friday" / specific date in next 3 days → P1.
     - "this month" / no date / generic action → P2.
     - "someday" / "maybe" / "future" → P3.
   - **due_date** (ISO 8601 date or datetime; null if not scheduled).
   - **related_domain** (slug from `domains-index.yaml`; infer from task content if obvious).
   - **related_asset / related_account / related_appointment / related_bill** (optional cross-references).
   - **tags** (list of short labels).
   - **source** = `"user"` for user-added, otherwise the skill / ingestor that created it.
2. Generate **id** = `task-<YYYYMMDD>-<NNN>` where NNN is the next sequential 3-digit number for today.
3. Set **status** = `open`, **created** = now (ISO 8601 with TZ).
4. Append the new record.

### List tasks

When the user asks to list / show tasks:

1. Filter to relevant statuses (default: `open` + `in_progress`; include `done` only on request).
2. **Group** by **priority** (P0 first).
3. Within each group, sort by **due_date** (soonest first; null last).
4. **Overdue**: compute today in the user's timezone. Tasks with `due_date < today` are overdue — list them first within their priority group with an `OVERDUE` prefix.
5. If any P0 tasks exist, surface them prominently at the top.
6. Filter further per user request (`--domain Health`, `--project tax-2026`, `--asset blue-camry`, `--tag taxes`).

### Complete a task

When the user asks to mark complete / close a task:

1. Resolve by **id** or unambiguous title match. Confirm if multiple match.
2. Set `status: done`, `completed_date: now`.

### Update a task

When the user asks to change a task:

1. Resolve by id or unambiguous match.
2. Modify only the **specified fields**. Do not clear unspecified fields unless the user asked.
3. If the update corrects how the task was captured (wrong inferred priority, mis-worded title, wrong scope or due date), also capture it to `_memory/action-signals.yaml` same-turn (`kind: correction`) per `contracts/capture.md` § 7.2a.

### Triage overdue

When the user asks to triage / clean up / deal with overdue tasks (`todo triage`; also the pointer `daily-update` and `whatsup` print when the overdue count is above zero):

1. Select `status in [open, in_progress]` AND `due_date < today`. Optional filter: `--priority Px` (or "only the P1s" in prose) keeps that bucket only — announce it (`Triaging overdue P1 tasks: <count>.`). Sort P0 first, then by days overdue. Empty queue → "Nothing overdue. You're caught up." and stop.
2. For each row, in order, force one decision — present the row and the menu:

   ```
   [<position> / <total>] <id> {priority} <title>
      Due: <date> ({N} days overdue) · Domain: <related_domain or "—">
      <description, truncated to 200 chars>

   d - Done (already complete; just hadn't logged)   r - Reschedule (new due date)
   p - drop Priority (to P3 / someday)               x - cancel (one-line reason)
   l - delegate (who? → note on their contact row)   s - skip (re-surface next time)
   ```

   Apply: **d** → `status: done`, `completed_date: now` (ask whether a follow-on task is needed when the description implies one). **r** → new `due_date`. **p** → `priority: P3` (or P2 if that fits better). **x** → `status: cancelled`, reason in `notes`. **l** → `status: cancelled`, `notes: "delegated to <name>"`, and append "delegated <task>" to that contact's `notes` in `contacts.yaml` when they have a row. **s** → untouched.
3. Persist after each decision (step 4), sync the scoped views (step 5), and end with one line: `Triaged: <total> [in Px]. Done: <d>, Rescheduled: <r>, Deprioritized: <p>, Dropped: <x>, Delegated: <l>, Skipped: <s>.` The log row (step 7) uses `action: "triage_overdue"` — the cadence skills' "no triage pass in 30 days" nudge greps for it.

### No specific request

When invoked without a clear add/list/complete/update/triage action:

1. Compute:
   - **X** = open tasks (count).
   - **Y** = open tasks overdue.
   - **Z** = tasks completed this week.
2. Print this summary and ask what they want to do next.

## 4. Persist

1. Write changes back to `workspace/_memory/todo.yaml`. Preserve formatting, comments, and schema_version.

## 5. Sync `status.md` / `todo.md` files

After any add / complete / update / triage operation, update the relevant scoped views per the sync contract (`contracts/task-management.md` § 5.1):

1. For each task touched, resolve `related_project` then `related_domain` to update the per-scope status files:
   - `related_project` set → rewrite `workspace/Projects/<slug>/status.md` (priority/due-date sort; per-project burn-down updated).
   - `related_domain` set (and no project) → rewrite `workspace/Domains/<Domain>/status.md`.
   - When BOTH set: render in the project's status.md (project takes precedence as the "doing" surface), AND surface a one-bullet summary in the related domain's `status.md` § Next Steps.
2. ALWAYS rewrite `workspace/todo.md` on every change — it is the unified all-tasks view across every project and domain. The Scope column uses operational-handle form (`project:<slug>` or `domain:<id>`) to disambiguate each row.
3. Format as markdown tables, grouped by priority, with a separate `Done` block at the bottom (per `templates/todo.md`):

   ```
   ## P0 — Today / Urgent

   | ID | Task | Due | Scope |
   |----|------|-----|-------|
   | task-NNN | Title text | Mon DD | project:<slug> |
   ```

   - Within each priority block, sort by due date (soonest first; no-date rows last).
   - Use `—` for missing due dates / scopes.
   - Omit empty priority blocks.
3. Update the `_Last updated:_` timestamp at the top of each modified file.
4. If a `status.md` doesn't exist for a scope, create from `superagent/templates/domains/status.md`.

## 6. P0 highlighting

If any P0 tasks exist and are still open after the operation:

1. End the response with a highly visible block:

   ```
   ## P0 — immediate attention
   - **task-NNN** Title — due Mon DD
   ```

2. This applies even when the user only asked for a summary or a partial update.

## 7. Logging

Append to `interaction-log.yaml`:

```yaml
- id: "<ilog-YYYY-MM-DD-NNN via uv run python -m superagent.tools.next_id --kind ilog --file _memory/interaction-log.yaml>"
  ts: "<ISO 8601 datetime with offset>"
  skill: "todo"
  action: "<add|list|complete|update>_task"   # or "triage_overdue" for the triage mode
  summary: "<add|list|complete|update> — <task title or count>"   # triage: "Triaged <N> overdue tasks: <breakdown>."
  related_domain: <if applicable>
  action_items: []
```
