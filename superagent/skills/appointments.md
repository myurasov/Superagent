---
name: superagent-appointments
description: >-
  Add an appointment (doctor, dentist, vet, mechanic, hairdresser, school
  event, in-person meeting, telehealth call) to `appointments.yaml` — with an
  auto-created P1 prep task when there are prep notes — then list upcoming
  appointments, prep for one (assemble briefing context), mark complete
  (with outcome + follow-ups), reschedule, cancel.
triggers:
  - appointments
  - upcoming appointments
  - prep for <appointment>
  - mark <appointment> done
  - reschedule <appointment>
  - add an appointment
  - book an appointment
  - schedule a <doctor / dentist / vet / mechanic> visit
  - book a <doctor / dentist / vet / mechanic> visit
  - book the dentist / doctor / vet / mechanic
  - I have an appointment with <provider>
mcp_required: []
mcp_optional:
  - calendar ingestors (gcal / icloud_calendar / outlook_calendar)
cli_required: []
cli_optional: []
---

<!-- step-index:start -->

## Step index

_Auto-generated by `tools/add_step_index.py`. Re-run on any edit._

| # | Step | Lines |
|---|------|-------|
| 1 | Branch on intent | 54-208 |
| 2 | Sync downstream | 209-215 |
| 3 | Sources | 216-218 |

<!-- step-index:end -->

# Superagent appointments skill

> **Pre-flight (lazy-materialization contract — `contracts/domains-and-assets.md` § 6.4a)**:
> The Add mode writes `Domains/<Domain>/rolodex.md` (provider row) and
> `Domains/<Domain>/status.md` § Next Steps; Mark complete writes
> `Domains/<Domain>/history.md`. Before any of these, run:
>
>     uv run python -m superagent.tools.domains ensure <domain-id>
>
> Idempotent. The domain is the appointment's `domain` field (`health`,
> `pets`, `vehicles`, `home`, `family`, ...).

## 1. Branch on intent

### List (default)

1. Read `_memory/appointments.yaml`.
2. Filter `status: scheduled` AND `start >= today`.
3. Sort ascending by `start`.
4. Format: `{date} {time} — {title} with {provider}, for {member} ({location})`.
5. Cap at 14 days unless `--all` or `--upcoming-days N` passed.

### Add

User asked to add / book / schedule an appointment ("add an appointment", "book an appointment", "schedule a <doctor / dentist / vet / mechanic> visit", "I have an appointment with <provider>"):

1. **Collect the appointment's data** (ask only for what the trigger did not already supply):
   - **Title** (short — e.g. "Annual cleaning", "30k mile service", "Annual physical").
   - **Kind**: medical | dental | vision | mental_health | vet | mechanic | home_repair | beauty | school | legal | financial | personal | other.
   - **Start** (ISO 8601 datetime with TZ).
   - **End** (optional).
   - **Location** (street + city; "telehealth"; "phone"; "<provider's office>").
   - **Provider** — contact id from `contacts.yaml`. Offer to invoke `add-contact` if missing.
   - **For member** — "self" | name (spouse, child, pet — pets are in `contacts.yaml` with role: pet).
   - **Domain** — Health (medical / dental / vision / mental_health) | Pets (vet) | Vehicles (mechanic) | Home (home_repair) | Family | etc.
   - **Asset** (id from `assets-index.yaml` — for vehicle service or appliance repair).
   - **Purpose** — one-liner.
   - **Prep notes** — what to bring, things to discuss, questions to ask.
   - **Confirmation** — provider's confirmation number.
   - **Recurrence** — if this is a recurring appointment, set `{ interval: <e.g. "annual">, n: 0 }`.

   Auto-derive **id** = `appt-<YYYYMMDD>-<provider-slug or kind-slug>`. Before appending, grep `_memory/appointments.yaml` for a `scheduled` row with the same provider and start date; if one exists, offer Reschedule instead of adding a duplicate.
2. **Append the appointment row** to `_memory/appointments.yaml.appointments[]` (shape per `templates/memory/appointments.yaml`; provenance per `contracts/provenance.md`):

   ```yaml
   - id: "<id>"
     title: "<Title>"
     kind: "<kind>"
     start: <start>
     end: <end or null>
     location: "<location>"
     provider: <contact-id or null>
     for_member: "<member>"
     domain: "<domain>"
     asset: <asset-id or null>
     purpose: "<purpose>"
     prep_notes: "<prep_notes>"
     prep_done: false
     status: "scheduled"
     completed_at: null
     outcome: ""
     followup: []
     confirmation: "<confirmation>"
     reminder_sent: false
     recurrence: <recurrence or null>
     external_id: null
     ingest_source: "manual"
     tags: []
     notes: ""
     provenance:
       source: "user"
       at: <now>
     created: <now>
     last_updated: <now>
   ```
3. **Auto-create prep task (if applicable)** — if `prep_notes` is non-empty, create a P1 task in `todo.yaml`:
   - Title: "Prep for <Title> on <date>"
   - Due date: <start> minus 1 day
   - related_appointment: <id>
   - Description: "<prep_notes>"
4. **Update domain rolodex** (after the pre-flight `domains ensure`) — if `provider` is set, ensure the provider's row in `Domains/<Domain>/rolodex.md` exists; update `Last contacted` to the appointment date.
5. **Update domain status** — add a bullet to `Domains/<Domain>/status.md` § Next Steps:

   > "<Title> with {provider} on {start} ({location})"
6. **World graph** (`contracts/world-graph.md` § 3 — write only edges `rebuild` derives): `tools/world.py.ensure_node` for `appointment:<slug>`; `ensure_edge` `scoped` → `domain:<domain>` (from the row's `domain`), `provider` → `contact:<provider>`, `related_asset` → `asset:<asset>` when set, and `for_member` → `contact:<id>` only when `for_member` is a contact id (never for `self` / `household`). A skipped call is repaired by the next `world rebuild`.
7. **Domain reflection** (`contracts/domain-reflection.md`): `uv run python -m superagent.tools.render_domain --domain <domain>` so the domain's derived blocks and `status.md` task tables reflect the new row.
8. **Confirm**:

   ```
   Added appointment "<Title>" on <start>.
   Provider: <name>. Location: <location>.
   Prep task: <"created task-NNN" or "no prep">.
   ```
9. **Log** — append to `interaction-log.yaml` (this is the § 2 row for the Add intent):

   ```yaml
   - id: "<uv run python -m superagent.tools.next_id --kind ilog --file _memory/interaction-log.yaml>"
     ts: <now ISO 8601 with offset>
     skill: "appointments"
     action: "add_appointment"
     participants: [<provider name if any>]
     summary: "Added <kind> appointment with <provider> on <date>."
     related_domain: <domain>
     related_asset: <asset-id>
     action_items: ["Prep for <Title> on <date>"] or []
   ```

### Prep

User asked to prep for an appointment:

1. Resolve appointment by id or fuzzy name match.
2. Assemble a prep brief:
   - **Provider context**: from `contacts.yaml.<provider>` — last contacted date, prior visit notes (search `Domains/<domain>/history.md`).
   - **Member context**:
     - For health appointments: from `health-records.yaml` for that `for_member` — active conditions, current meds, any open symptoms in last 30 days, any abnormal results not yet followed up.
     - For vehicle appointments: from `assets-index.yaml.<asset>` — last service date, mileage, open maintenance items.
     - For pet appointments: from `assets-index.yaml.<asset>` (pets stored as assets) — vaccination status, current meds, food.
   - **Prep notes**: from `appointments.yaml.<appt>.prep_notes`.
   - **Questions to ask**: extract from prep_notes + any open follow-ups from `appointments.yaml.history[]` matching this provider.
   - **What to bring**: heuristic by kind — insurance card for medical/dental/vision; med list for medical; vehicle title for some mechanic visits; pet vaccine record for vet; pay-stubs for financial; etc.
3. Render the brief as a printable markdown doc into `workspace/Outbox/appointments/<id>-prep.md`.
4. Set `appointments.yaml.<appt>.prep_done: true`.
5. If a P1 prep task exists in `todo.yaml` for this appointment, mark it `done`.

### Mark complete

User said "I just got back from / had <appointment>":

1. Resolve appointment.
2. Ask:
   - **Outcome** — free text. What happened, what was decided, what they said.
   - **Medications changed** (yes / no, only for medical / vet appointments).
   - **Follow-ups** — list of `{description, due_date}` items.
   - **Copay / payment** — if the appointment involved an out-of-pocket payment, capture the receipt per `contracts/payment-confirmations.md` and record the saved path in `appointments.yaml.<appt>.confirmation_ref`.
3. Set `status: completed`, `completed_at: now`, `outcome: <text>`, `followup: <list>`.
4. For each follow-up, create a P2 task in `todo.yaml` with `related_appointment: <id>`.
5. Append to the relevant `Domains/<Domain>/history.md`:

   ```markdown
   #### <YYYY-MM-DD> — <Title> with <provider>

   <outcome>

   Follow-ups:
   - [ ] <description> (due <date>)

   ---
   ```
6. If `kind` is medical / dental / vision / mental_health / vet, also append a row to `_memory/health-records.yaml.visits[]` per its schema.
7. **Recurrence**: if `recurrence` is non-null, auto-create the next occurrence in `appointments.yaml` (set `start = completed_at + interval`, `status: scheduled`, `prep_done: false`).

### Reschedule

User asked to move an appointment:

1. Resolve appointment.
2. Ask new `start`.
3. Update `start`, set `status: scheduled` (if it was `rescheduled` already).
4. Re-emit any prep task with the new due date.

### Cancel

1. Resolve appointment.
2. Set `status: cancelled`, `notes` += "<reason>".
3. Mark any prep task `cancelled`.

## 2. Sync downstream

After any operation:
- Update `Domains/<Domain>/status.md` § Next Steps (remove cancelled / completed; add new if rescheduled).
- Update `Domains/<Domain>/rolodex.md` row for the provider with `Last contacted` ← appointment's completed_at.
- Append to `interaction-log.yaml` (the Add intent's row shape is in § 1 Add step 9; the other intents use the same canonical shape with `action: "prep_appointment"`, `"complete_appointment"`, `"reschedule_appointment"`, or `"cancel_appointment"`).

## 3. Sources

Calendar ingestors push items into `appointments.yaml` automatically when their detection heuristic fires (per `contracts/ingestion.md` ingestor obligations). Skill prefers local data; calls live calendar only for the strictly-newer slice on `--refresh`.
