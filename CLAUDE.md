# Superagent — Claude Code loader

Claude Code reads this file on every turn. Superagent's canonical always-on instructions live in [`AGENTS.md`](AGENTS.md); the `@`-import below pulls them in so a single document drives behavior under both Cursor and Claude Code. **Edit `AGENTS.md`, not this file.**

@AGENTS.md
